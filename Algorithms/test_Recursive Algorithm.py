from unittest import TestCase
from recur
import numpy as np
class Test(TestCase):
    def test_create_pairs(self):
        arr = np.array([[1, 2, 3], [4, 5, 6]])
        self.assertEqual(ra.test_create_pairs(arr)==)
